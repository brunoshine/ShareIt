@{
    ViewBag.Title = "Index";
}

<h2>Debug</h2>

<table>
    @foreach (KeyValuePair<string,string> item in ViewBag.appSettings)
    {
        <tr>
            <td>@item.Key</td>
            <td>@item.Value</td>
        </tr>
    }
</table>